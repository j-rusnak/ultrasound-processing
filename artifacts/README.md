# Artifacts Directory

This directory is intentionally gitignored except for this README.

Use it for generated checkpoints, predictions, metrics, Grad-CAM outputs, and plots:

```text
artifacts/checkpoints/
artifacts/results/
```

Historical `.pt` files from Google Drive can be placed under `artifacts/checkpoints/`.
